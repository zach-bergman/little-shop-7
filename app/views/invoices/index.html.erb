<%= render "shared/merchant_navbar"%>

<div class="invoice-id"
  <h1>My Invoices</h1>
</div>

  <div class="column-left">
    <div class="invoices-list">
      <% @invoices.each do |invoice| %>
        <%= link_to "Invoice #{invoice.id}", merchant_invoice_path(@merchant, invoice.id) %>
      <% end %>
    </div>
  </div>
